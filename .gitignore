# Dependencies
node_modules/

# Build / generated by the design-system compiler — do NOT commit by hand,
# but DO commit the generated bundle/manifest if consumers fetch them directly.
# (Left untracked here; regenerate on build.)
_ds_bundle.js
_ds_manifest.json
_adherence.oxlintrc.json

# Bundling / export artifacts — export/bloqr-app.html is committed as the
# primary deliverable; other export/ artifacts (e.g. landing) remain untracked.
export/*
!export/bloqr-app.html

# Editor / OS
.DS_Store
*.log
.vscode/
.idea/
